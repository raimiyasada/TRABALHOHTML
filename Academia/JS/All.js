/*const botao= document.getElementById("BtnAcess") 

botao.addEventListener("click" ,EntBtn)
     function EntBtn(){ 
     document.location = 'SignForm.html';
     return true;
}

function EntBt(){
    if(Login=="zzz@gmil.com" && Senha=="zzz"){
        document.location = 'SignForm.html';
        return true;
    }
}
const botao= document.getElementById("BtnAcess") 

botao.addEventListener("click" ,EntBtn)

    const Login=document.LoginForm.elements["Login"].value;
    const Senha=document.LoginForm.elements["Senha"].value;

    if(Login== "zz" && Senha==1){
        /*document.location='SignForm.html'
        alert(validado)

    }
    else{
        alert("colocar a senha valida")
    }*/

    /*a função dos botões que estão no formulario do cadastro do aluno*/
    const botao= document.getElementById("backbtn") 

botao.addEventListener("click" ,getback)

function getback(){
    document.location = 'Aluno.html';
    return true;
}
const bouton= document.getElementById("savebtn") 

bouton.addEventListener("click" ,sauver)

function sauver(){
    window.alert(`A soma entre ${n1} e ${n2} é igual a ${s}`);
    return true;
}